/** @type {import('next').NextConfig} */
// Déclaration du type de configuration pour Next.js

const nextConfig = {
    compiler: {
      styledComponents: true, // Activation de la prise en charge de styled-components
      // ... Autres configurations du compilateur
    },
  }
  
  module.exports = nextConfig
  // Exportation de la configuration pour l'utiliser dans votre projet Next.js
  